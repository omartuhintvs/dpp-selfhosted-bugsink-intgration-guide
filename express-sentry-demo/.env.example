# Express + Sentry/Bugsink Configuration
# Copy this file to .env and fill in your actual values

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Sentry/Bugsink Configuration
# Get this from your Bugsink dashboard: Settings > Projects > [Your Project] > Client Keys (DSN)
# REQUIRED: Application will not send errors without this
SENTRY_DSN=https://YOUR_PUBLIC_KEY@your-bugsink-domain.com/PROJECT_ID

# Performance Monitoring (optional)
# Defaults: 10% in production, 100% in development
# Uncomment to override:
# SENTRY_TRACES_SAMPLE_RATE=0.1
# SENTRY_PROFILES_SAMPLE_RATE=0.1

# Security Notes:
# - NEVER commit your actual .env file with real DSN to version control
# - Add .env to your .gitignore (already done)
# - Rotate DSN if accidentally exposed
# - Use environment-specific DSNs (dev/staging/prod)
